<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://fonts.googleapis.com/css?family=Noto+Sans+JP:400,700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.0/css/all.min.css" integrity="sha384-KyZXEAg3QhqLMpG8r+Knujsl5z7X9z5l/5VQU8IdjwD5Vbj9UfjcB7VW5Z8X3xV9" crossorigin="anonymous">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.1/aos.css" rel="stylesheet">
  <style>
    body {
      font-family: 'Noto Sans JP', sans-serif;
      color: #333;
      line-height: 1.6;
      margin: 0;
      background-image: url(<%= asset_url 'Creator.jpg' %>);
      /* 背景画像の表示方法を指定する場合は、background-size, background-positionなどのプロパティを追加することができます */
      background-size: cover; /* 画面いっぱいに表示 */
      background-position: center center; /* 画像を中央に表示 */
      /* その他のスタイルを追加 */
      min-height: 100vh; /* 追加 */
      padding-top: 100px; /* 追加 */
    }

    
  </style>
  <title>Writer's Base</title>
</head>
<body>
  <div id="top" class="big-bg">
    <div class="top-content_title" data-aos="fade-up">
      <br>
      <h2>Writer's Base へようこそ</h2>
      <hr>
    </div>
    <div class="top-content" data-aos="fade-up" data-aos-delay="300">
      <p><%= t("writers_base.description") %></p>
      <p><%= t("writers_base.question") %></p>
      <p><%= t("writers_base.solution") %></p>
      <p><%= t("writers_base.collaboration") %></p>
      <p><%= t("writers_base.community") %></p>
      <p><%= t("writers_base.invitation") %></p>
    </div>
    <div class="top-content" data-aos="fade-up" data-aos-delay="600">
      <h3><%= t("app_features.title") %></h3>
        <div style="display: flex; justify-content: center; flex-wrap: wrap;">
          <div class="spacing">
            <i class="fas fa-code-branch icon"></i>
            <p><%= t("app_features.version_control") %></p>
            <p><%= t("app_features.diff_display") %></p>
          </div>
          <div class="spacing">
            <i class="fas fa-tasks icon"></i>
            <p><%= t("app_features.issue_management") %></p>
          </div>
        </div>
    </div>
<!--------未記述 追記修正予定------------>
  </div>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.1/aos.js"></script>
  <script>
    AOS.init({
      duration: 1000,
      easing: 'ease-in-out',
      once: true,
    });
  </script>
</body>
</html>