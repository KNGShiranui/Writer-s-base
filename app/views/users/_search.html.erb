<%= search_form_for @q, url: url, html: { class: 'search-form' } do |f| %>
  <%= f.search_field :name_or_content_cont, class: 'form-control', placeholder: t('common.search word') %>
  <%= f.submit t('common.search'), class: 'btn btn-primary' %>
<% end %>
